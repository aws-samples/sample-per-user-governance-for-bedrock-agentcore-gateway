# Fully pinned dependency set for the Claude Code runtime image.
#
# Resolved for the platform this image is built for (CPython 3.12, linux/arm64).
# The image needs only the AgentCore Runtime SDK; the agent itself is the
# native Claude Code CLI, pinned separately by CLAUDE_CODE_VERSION in the
# Dockerfile.
#
# Direct dependency, and the range these pins were resolved from:
#   bedrock-agentcore >=0.1,<2
#
# To move the pins forward, re-resolve on the build platform and replace
# everything below this header. The ranged install below is a one-off
# resolution step in a throwaway container whose only output is the pip freeze
# list: the range MUST NOT appear in this file, in a Dockerfile, or in any
# build or deploy step. What the image installs is the pinned set below, and it
# is not updated until that set is replaced and committed.
#
#   docker run --rm --platform linux/arm64 \
#     public.ecr.aws/docker/library/python:3.12-slim sh -c '
#       pip install --quiet --no-cache-dir "bedrock-agentcore>=0.1,<2" && pip freeze'
#
# Then rebuild and invoke the runtime before pushing: the range permits majors
# this app was never run against.
annotated-types==0.8.0
anyio==4.14.2
bedrock-agentcore==1.22.0
boto3==1.43.80
botocore==1.43.80
click==8.4.2
h11==0.16.0
idna==3.19
jmespath==1.1.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dateutil==2.9.0.post0
s3transfer==0.19.2
six==1.17.0
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
websockets==17.0.1
